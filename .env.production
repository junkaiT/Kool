# Production-only config, committed to the repo and loaded during `next build`
# (NOT during `next dev`, so local development stays free of analytics traffic).
# ONLY put public NEXT_PUBLIC_* values here — never secrets. Vercel dashboard
# env vars override anything set here.

# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_ID=G-0PRT7WTCW1

# Meta Pixel / dataset ID — "KoolAircon CRM" dataset in Events Manager
NEXT_PUBLIC_META_PIXEL_ID=1729780851492366
